# Chat and image generation use Pollinations.ai, which is free and keyless —
# no setup needed for those.

# Optional: free Hugging Face token to enable video generation.
# Create one for free at https://huggingface.co/settings/tokens
HF_API_TOKEN=
