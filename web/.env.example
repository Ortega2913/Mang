# Optional: enables the experimental "Pro" text-to-video model via Hugging Face's
# free Inference API. Get a free token at https://huggingface.co/settings/tokens
# (read access is enough). Without this, the site still works fully using the
# built-in AI Slideshow video generator (no key required).
HF_API_TOKEN=
